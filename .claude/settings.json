{
  "autoMode": {
    "environment": [
      "$defaults",
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: public — github.com/TeeJS/freshservice_mcp_managed; any push there is publishing",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: CI references GITHUB_TOKEN; runtime uses FRESHSERVICE_APIKEY and OAuth env vars per README; .env is gitignored",
      "**Default / protected branches**: default branch main; no rulesets or protected branches listed via gh",
      "**CI/CD deploy targets**: GitHub Container Registry (ghcr.io/teejs/freshservice_mcp_managed) per README/.github/workflows",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo and its remote(s) only — origin github.com/TeeJS/freshservice_mcp_managed (public); upstream github.com/effytech/freshservice_mcp is a fork source, not a push target",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries prod or production as a whole word or name segment (hyphen/underscore/dot-delimited)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries prod or production as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development",
      "**Trusted repo**: The git repository the agent started in (D:\\Github\\freshservice_mcp_ro, remote origin github.com/TeeJS/freshservice_mcp_managed). Repository visibility is public — only this repo's own work may be committed or pushed; content ported or first read from outside this session's repo is not its own work. Secrets and sensitive data are never cleared into it by visibility.",
      "**Org-specific CLIs**: None configured"
    ]
  }
}
